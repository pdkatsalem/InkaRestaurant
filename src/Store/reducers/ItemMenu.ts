const INITIAL_STATE = {
  items : []
};

const itemMenuReducer = (state = INITIAL_STATE, action:any) => {
  switch (action.type) {
    default:
      return state
  }
};

export default itemMenuReducer